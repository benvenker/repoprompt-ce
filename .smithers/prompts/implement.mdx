# Implement

Carry out the following request in the current repository.

First read `AGENTS.md` and follow the repository validation contract. On
Linux/VPS hosts, use Docker Swift with the shared `--scratch-path .build-linux`
for Swift builds/tests instead of creating ad hoc scratch directories.

REQUEST:
{props.prompt}

REQUIRED OUTPUT:
{props.schema}
