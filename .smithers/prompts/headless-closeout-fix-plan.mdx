Plan the smallest safe fix pass for the current blocking findings.

Mandatory Agent Mail coordination:
- Use the `agent-mail` skill/reference before doing this task.
- At session start, resolve the current repository root and call `macro_start_session(human_key="<current repository root>", program="codex", model="<actual Codex model label available in this session, or unknown if unavailable>", task_description="headless-closeout-grinder fix-plan")`.
- Check the returned inbox and announce start/completion in thread `headless-closeout-review-findings-2026-06-16`.
- This is read-only; do not reserve files unless you unexpectedly need to edit. If Agent Mail MCP tools are unavailable, report that and stop.

Use only the actionable P0/P1/P2 findings in the normalized review. Do not expand scope to P3/nits unless they are inseparable from a blocker fix. Do not edit files in this task.

Before the next task edits, it must use Agent Mail reservations for every target file.

Scope packet:
{JSON.stringify(props.scope, null, 2)}

Source plans:
{JSON.stringify(props.sourcePlanPaths, null, 2)}

Current normalized review:
{JSON.stringify(props.normalizedReview, null, 2)}

Operator note:
{props.operatorPrompt}

REQUIRED OUTPUT:
{props.schema}
