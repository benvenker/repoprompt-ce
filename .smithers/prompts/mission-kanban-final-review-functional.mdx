# Final Mission Review: Functional Completeness

Holistically review the integrated campaign against the original source plan with focus on functional completeness, missing requirements, and behavior required by the plan.

Approve only when the integrated results satisfy the plan's implementation units and acceptance examples. Identify follow-up ticket specs for missing functional work.

Plan summary:
{JSON.stringify(props.planSummary, null, 2)}

Materialized tickets:
{JSON.stringify(props.materializedTickets, null, 2)}

Ticket results:
{JSON.stringify(props.ticketResults, null, 2)}

Integration result:
{JSON.stringify(props.integrationResult, null, 2)}

REQUIRED OUTPUT:
{props.schema}
