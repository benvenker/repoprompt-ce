# Validate

Validate the current repository state for the following request.

First read `AGENTS.md` and follow the repository validation contract. On
Linux/VPS hosts, use Docker Swift with the shared `--scratch-path .build-linux`
for Swift builds/tests instead of creating ad hoc scratch directories. Reuse
fresh successful Docker build evidence for the exact tree when the request
provides it, unless you identify a concrete reason to rerun validation.

REQUEST:
{props.prompt}

REQUIRED OUTPUT:
{props.schema}
