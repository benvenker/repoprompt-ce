# Final Mission Review: Architecture

Holistically review the integrated campaign against the original source plan with focus on architecture, RepoPrompt CE source layout, operational risks, and maintainability.

Approve only when the campaign follows repository ownership boundaries and does not introduce avoidable operational risk. Identify follow-up tickets for architecture or source-layout issues.

Plan summary:
{JSON.stringify(props.planSummary, null, 2)}

Materialized tickets:
{JSON.stringify(props.materializedTickets, null, 2)}

Ticket results:
{JSON.stringify(props.ticketResults, null, 2)}

Integration result:
{JSON.stringify(props.integrationResult, null, 2)}

REQUIRED OUTPUT:
{props.schema}
